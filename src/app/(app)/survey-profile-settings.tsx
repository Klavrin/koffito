import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SurveyProfileSettingsPage() {
  return (
    <SafeAreaView>
      <Text>Survey profile settings page</Text>
    </SafeAreaView>
  );
}
